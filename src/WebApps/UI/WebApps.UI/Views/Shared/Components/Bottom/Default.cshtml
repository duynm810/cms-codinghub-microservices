@model BottomViewModel
@{
    var postsWithCategory = Model.PostsWithCategory.ToList();
}

<div class="container">
    <div class="row">
        @if (postsWithCategory.Count != 0)
        {
            foreach (var postItem in postsWithCategory)
            {
                <div class="col-lg-4 col-md-6">
                    <div class="sidebar-widget widget-latest-posts mb-30">
                        <div class="widget-header-2 position-relative mb-30">
                            <h5 class="mt-5 mb-30">@postItem.Category.Name</h5>
                        </div>
                        <div class="post-block-list post-module-1">
                            <ul class="list-post">
                                @if (postItem.Posts != null)
                                {
                                    foreach (var item in postItem.Posts)
                                    {
                                        var postUrl = "/post/" + item.Slug;

                                        <li class="mb-30">
                                            <div class="d-flex hover-up-2 transition-normal">
                                                <div class="post-thumb post-thumb-80 d-flex mr-15 border-radius-5 img-hover-scale overflow-hidden">
                                                    <a class="color-white" href="@postUrl">
                                                        <img src="@item.Thumbnail" alt="@item.Thumbnail">
                                                    </a>
                                                </div>
                                                <div class="post-content media-body">
                                                    <h6 class="post-title mb-15 text-limit-2-row font-medium">
                                                        <a href="@postUrl">@item.Title</a>
                                                    </h6>
                                                    <div class="entry-meta meta-1 float-start font-x-small text-uppercase">
                                                        <span class="post-on">@(item.PublishedDate.HasValue ? item.PublishedDate.Value.ToString("dd MMMM", CultureInfo.InvariantCulture) : "--")</span>
                                                        <span class="post-by has-dot">@item.ViewCount views</span>
                                                    </div>
                                                </div>
                                            </div>
                                        </li>
                                    }
                                }
                            </ul>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>
<!--container-->