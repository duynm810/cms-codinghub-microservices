@using Microsoft.Extensions.Configuration
@inject IConfiguration Configuration
@{
    Layout = "_Layout";
}

@{
    var server = Configuration["ApiSettings:ServerUrl"];
    var serverUrl = $"{server}";
    ViewBag.ServerUrl = serverUrl;
}